extends layout

block content
  div.profile-container
    h1 All Users
    each user in users
      div.user-card
        h2= user.name
        p Email: #{user.email}
        p Course: #{user.course}
        a.btn(href="#") View Profile
extends layout

block content
  div.profile-container
    a.btn(href="/preview/societies") Back to Societies

    h1= society.email
    p= society.about_us_text
    button.btn Join Society
    button.btn RSVP to Event

    div.bio-section
      h3 Society Details
      p Email: #{society.email}

    div.bio-section
      button.btn(onclick="toggleMembers()") View Members
      div#members-list(style="display:none")
        h3 Members
        if members.length === 0
          p No members yet
        else
          each member in members
            div.user-card
              p #{member.first_name} #{member.last_name}
              p= member.email

    script.
      function toggleMembers() {
        var list = document.getElementById('members-list');
        if (list.style.display === 'none') {
          list.style.display = 'block';
        } else {
          list.style.display = 'none';
        }
      }
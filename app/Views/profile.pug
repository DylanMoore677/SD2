extends layout

block content
  div.profile-container
    h1 #{user.first_name} #{user.last_name}'s Profile
    img.profile-pic(src="https://via.placeholder.com/150", alt="Profile Picture")
    p Email: #{user.email}
    div.bio-section
      h3 About Me
      p Student at Roehampton University

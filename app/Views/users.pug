extends layout

block content
  div.profile-container
    h1 All Users
    each user in users
      div.user-card
        h2 #{user.first_name} #{user.last_name}
        p Email: #{user.email}
        a.btn(href="#") View Profile